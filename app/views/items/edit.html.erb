<div class="routine-banner"><h1>Edit Item: <%= @item.name %></h1></div>

<%= render partial: "layouts/errors", locals: {object: @item} %>

<%= render partial: "form", locals: {path: routine_item_path(@routine, @item)} %>
